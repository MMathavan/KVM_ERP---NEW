@{
    ViewBag.Title = "Issued Stock";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">
            <i class="fa fa-file-invoice"></i>
            Issued Stock
        </h2>
        @if (User.IsInRole("IssuedStockCreate"))
        {
            <a href="@Url.Action("Form", "IssuedStock")" class="btn btn-primary">
                <i class="fa fa-plus"></i> Add Issued Stock
            </a>
        }
    </div>

    <div class="card">
        <div class="card-body">
            <p class="text-muted mb-0">
                Issued Stock list screen placeholder. Configure grid / filters here as per your requirements.
            </p>
        </div>
    </div>
</div>
